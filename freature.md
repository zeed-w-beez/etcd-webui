# etcd-webui

本项目致力于打造一个安全、高效、易用且面向未来的 etcd 可视化管理平台。

## ✅ 1. **现代化 UI 与响应式设计**

- 采用 **React + TypeScript + Tailwind CSS** 构建，界面简洁、交互流畅
- 完全**响应式布局**，适配桌面、平板及大屏终端
- 基于 **ShadCN UI** 组件库，提供一致的视觉语言与无障碍（a11y）支持
- 深色/浅色主题自动切换，提升长时间使用的舒适度

## ✅ 2. **深度兼容 etcd 官方 API，版本对齐无偏差**

- 直接调用 **etcd v3 gRPC API**（通过 gRPC Gateway 或自研代理层）
- 支持 **etcd 3.4 ~ 3.6+** 主流版本，确保功能一致性（不支持 v2 API）
- 完整实现 **KV、Watch、Lease、Auth、Maintenance** 五大核心模块
- 自动识别集群版本，动态启用/禁用对应功能（如 compact、defrag）

## ✅ 3. **多集群统一管理，全局掌控 etcd 健康状态**

- 支持配置并切换**多个 etcd 集群**（开发 / 测试 / 生产环境）
- 实时展示每个集群的：
  - 节点列表与角色（Leader/Follower）
  - 当前 Raft Term 与 Revision
  - 存储大小、碎片率、QPS 指标（通过 `/metrics` 或 Maintenance API）
  - TLS 与认证状态
- 提供一键连接测试与健康检查（`/health`）

## ✅ 4. **可视化键值对操作，告别命令行繁琐输入**

- 树形结构展示 key 层级（自动按 `/` 分割路径）
- 支持 **创建、读取、更新、删除（CRUD）** 全生命周期操作
- 内置 **JSON/YAML/文本** 多格式编辑器，自动高亮与校验
- 批量导入/导出（支持 `.json` 或 `etcdctl dump` 格式）
- 操作历史记录 + 撤销（Undo）功能，防止误删

## ✅ 5. **实时事件监听（Watch），动态感知数据变化**

- 可对任意 key 或前缀开启 **Watch 监听**
- 实时滚动展示 **PUT / DELETE 事件**，包含：
  - 修改时间（基于 Revision）
  - 新旧值对比（Diff 视图）
  - Lease ID（如适用）
- 支持暂停/清空/保存 Watch 日志，便于调试分布式系统行为

## ✅ 6. **高性能大数据量处理，拒绝卡顿**

- 采用 **基于 key 的分页加载**（非 OFFSET），支持百万级 key 浏览
- 前端集成 **虚拟滚动（Virtualized List）**，仅渲染可视区域
- 默认限制无前缀查询，强制用户指定范围，保护 etcd 性能
- 后端代理层自动限流、超时控制，避免压垮 etcd

## ✅ 7. **企业级安全与权限控制**

- 支持 **etcd 用户名/密码认证** 与 **RBAC 权限模型**
- 敏感操作（如删除、修改）需二次确认
- 所有凭证由后端代理管理，**前端永不接触 etcd 密钥**

## ✅ 8. **开发者友好，易于部署与扩展**

- 提供 **Docker 镜像**，5 分钟快速启动
- 前后端分离架构，支持独立部署或一体化容器
- 开放 RESTful API，便于集成到 CI/CD 或监控平台
- 完整 TypeScript 类型定义，方便二次开发
